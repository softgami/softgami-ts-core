import { PasswordUtilsService } from './password-utils.service';

describe('PasswordUtilsService', () => {

    describe('validatePassword', () => {

        test('', () => {

            PasswordUtilsService.validatePassword('some password');

        });

    });

});
