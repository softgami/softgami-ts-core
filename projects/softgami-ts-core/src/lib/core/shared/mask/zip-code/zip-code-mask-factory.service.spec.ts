import { ZipCodeMaskFactoryService } from './zip-code-mask-factory.service';

describe('ZipCodeMaskFactoryService', () => {

    const service: ZipCodeMaskFactoryService = new ZipCodeMaskFactoryService();

    test('', () => {

        expect(service).toBeDefined();

    });

});
