export enum Types {
    STRING = 'string',
    NUMBER = 'number',
    DECIMAL = 'decimal',
    BOOLEAN = 'boolean',
    DATE = 'date',
    OBJECT = 'object',
    ARRAY = 'array',
    DECIMAL128 = 'decimal128',
    ENUM = 'enum',
}
