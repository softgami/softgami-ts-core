import { DateRegexFactoryService } from './date-regex-factory.service';

describe('DateRegexFactoryService', () => {

    const service: DateRegexFactoryService = new DateRegexFactoryService();

    test('', () => {

        expect(service).toBeDefined();

    });

});
