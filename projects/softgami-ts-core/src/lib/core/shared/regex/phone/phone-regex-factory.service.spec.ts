import { PhoneRegexFactoryService } from './phone-regex-factory.service';

describe('PhoneRegexFactoryService', () => {

    const service: PhoneRegexFactoryService = new PhoneRegexFactoryService();

    test('', () => {

        expect(service).toBeDefined();

    });

});
