import { ZipCodeRegexFactoryService } from './zip-code-regex-factory.service';

describe('ZipCodeRegexFactoryService', () => {

    const service: ZipCodeRegexFactoryService = new ZipCodeRegexFactoryService();

    test('', () => {

        expect(service).toBeDefined();

    });

});
